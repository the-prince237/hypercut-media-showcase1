import { Box } from '@mui/material';
import React from 'react';

const BluredRing = () => (
  <Box className='bg-gradientConic1 absolute left-[-150px] top-[50px] h-[300px] w-[300px] rounded-full' />
);

export default BluredRing;
